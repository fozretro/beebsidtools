/**
 * Browser keyCode values MachineSession.keyDown expects.
 * Local copy of the three jsbeeb utils.keyCodes we use (no src/utils.js import).
 */
export const keyCodes = {
  ENTER: 13,
  SHIFT: 16,
  DOWN: 40,
};
